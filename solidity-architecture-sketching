modifier checkNFTOwnership(uint tokenId) {
  //Checks to ensure that the NFT is owned by the msg.sender
}

function mintTwitterAccountNFT(bytes32 authtoken, string accountHandle) returns (uint tokenId) {
  //Mints an NFT for a Twitter account after the user has logged in via Twitter
  //These NFTs will have an expiry date and require users to "renew" them to ensure they still hold access to the account
}

function mintTweetNFT(bytes32 authtoken, string accountHandle, string tweetId) returns (uint tokenId) {
  //Mints an NFT for a Tweet from a user after the user has logged in via Twitter
}

function transfer(uint tokenId)  {
  //Allows user to transfer an NFT they own to another address.
  //This will utilize the EIP-721 NFT standard and leverage the transfer function in there
}

function renewTwitterAccountNFT(bytes32 authtoken, uint tokenId) returns (bool) {
  //Allows user to renew their Twitter account NFT if it has expired
}
